export enum SkinTones {
  Light = '#FFFFFF',
  Dark = '#BCBCBC',
}

export enum HairColors {
  Black = '#2B2B2B',
}

export enum AccentColors {
  Primary = '#2B2B2B',
}

export enum BackgroundColors {
  Default = '#FFFFFF',
}
